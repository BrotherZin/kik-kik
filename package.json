{
  "dependencies": {
    "axios": "^1.2.1",
    "less-loader": "^11.1.0",
    "underscore": "^1.13.6",
    "vue-typer": "^1.2.0"
  },
  "scripts": {
    "build": "cd front && npm install --force && npm run build && cd ../server && npm install --force && npm run start"
  }
}
